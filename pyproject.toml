[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bg_translations"
version = "0.0.1"
description = "Bulgarian translations for ERPNext"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["bg_translations*"]
